# Telegram
TELEGRAM_BOT_TOKEN=
IRON_SESSION_PASSWORD=change-me-at-least-32-chars-long
NEXT_PUBLIC_TELEGRAM_BOT_ID=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (Supabase Postgres URL or any Postgres)
# Prisma runtime should use the pooled URL (serverless-friendly).
DATABASE_URL=
# Prisma migrations should use a direct (non-pooled) URL.
DIRECT_URL=
